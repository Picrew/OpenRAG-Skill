# Expected Spec: normative_force_drift

Required sections:
- `Answer`
- `Evidence Map`
- `Conflicts or Gaps`

Required evidence IDs:
- `E1`
- `E2`
- `E3`

Required conclusions:
- Password length is mandatory
- Password rotation after suspected compromise is recommended, not mandatory
- Password manager registration is optional

Claims that must not appear:
- A statement that all three items are required
- A statement that password rotation is mandatory
- A statement that password manager registration is required or recommended unless explicitly supported

Special notes:
- `Shared Ground` must not flatten the force distinctions
- The answer should keep the force mismatch in `Differences`
